# Node modules & package logs
node_modules/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Build outputs (uncomment/modify if you want to keep 'dist' in repo)
dist/
build/
# If you want to keep frontend/dist in repo, remove or comment out the `dist/` line.

# Environment / secrets
.env
.env.local
.env.*.local

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs & coverage
logs/
*.log
coverage/
coverage/*

# Misc
*.tgz
*.zip
*.bak
*.tmp

# Optional: lock files (if you don't want them in repo)
# package-lock.json
# yarn.lock
